package main

import (
	"fmt"
)

// Pseudocode:
// Add constraints/parameter validation
// Create the "return string"
// Loop through each string in the input array
// Get the first byte of the first string. hold onto this for comparison to the first bytes in the other arrays
// If there is no match and our "return string" is empty, return an empty string.
// If there is no match and our "return string" is NOT empty, return the "return string".
// If there is a match, add the common byte to the "return string". Iterate again/repeat.



func longestCommonPrefix(strs []string) string {

}